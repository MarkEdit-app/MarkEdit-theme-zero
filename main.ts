import { overrideThemes } from 'markedit-theming';

overrideThemes({
  light: {
    colors: {
      allowsFallback: false,
    },
  },
  dark: {
    colors: {
      allowsFallback: false,
    },
  },
});
